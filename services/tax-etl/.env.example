# Z-34 local config — copy to .env, never commit .env
DB_PATH=./data/tax_lien.db
FEMA_DATA_DIR=./data/fema
LOG_LEVEL=INFO

# Optional: webhook for Tier A alerts (Discord, Slack, or custom)
ALERT_WEBHOOK_URL=

# Optional: skip tracing (PropStream, BatchSkipTracing)
SKIPTRACE_API_KEY=
