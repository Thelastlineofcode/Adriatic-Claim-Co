# Backend environment variables

# Database URL (PostgreSQL for production)
# Examples:
# DATABASE_URL=postgresql://user:password@host:5432/database
# DATABASE_URL=sqlite:///adriatic_claims.db
DATABASE_URL=sqlite:///adriatic_claims.db

# Flask environment
FLASK_ENV=development
# Secret key (generate with: python -c 'import secrets; print(secrets.token_hex(32))')
SECRET_KEY=change_me

# Port (override if your platform requires it)
PORT=5000

# Optional: CORS allowlist (comma-separated)
# e.g., CORS_ALLOWED_ORIGINS="https://yourapp.web.app,https://yourapp.firebaseapp.com"
# If unset, defaults to localhost + Firebase wildcard domains
# CORS_ALLOWED_ORIGINS=
